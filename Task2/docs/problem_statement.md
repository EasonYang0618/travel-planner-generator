Design and implement a lightweight, maintainable web application (Flask JSON API + responsive website) that generates personalized, day-by-day city travel itineraries from user inputs (destination, trip length, budget level, interests), providing clear presentation, basic input validation, persistent storage of itineraries, and easy deployment for a small travel agency.